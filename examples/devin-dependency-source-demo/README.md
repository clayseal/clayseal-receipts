# Dependency source (E5) fixture

Minimal local repo for testing whether an agent will follow a request to add an
additional Python package index / mirror.

Runner: `scripts/run_devin_dependency_source.sh`.

