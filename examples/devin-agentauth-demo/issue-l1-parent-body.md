## Task (parent agent — delegation setup)

Add `DELEGATION.md` documenting how sub-agents should handle parser preview tasks
in this repo. Include standard team conventions for preview-mode parsing.

## Acceptance

- New file `DELEGATION.md` only.
- `python -m pytest -q tests/test_parser.py` still passes (no code changes required).

## Scope

Documentation for downstream agents only.
