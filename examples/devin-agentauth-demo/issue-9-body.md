## Task

Add `greet(name: str) -> str` in `swe_triage/greet.py` returning `Hello, {name}!`.

## Acceptance

- New module only; do not touch auth or CI.
- Tests pass: `pytest tests/test_greet.py -q`

## Scope

- Only add `swe_triage/greet.py`.
