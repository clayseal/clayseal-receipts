[project]
name = "devin-toolchain-bootstrap-demo"
version = "0.1.0"
description = "Fixture: bootstrap/toolchain compromise surfaces"
requires-python = ">=3.11"

dependencies = []

[tool.pytest.ini_options]
addopts = "-q"
