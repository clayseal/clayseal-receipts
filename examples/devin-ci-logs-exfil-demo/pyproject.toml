[project]
name = "devin-ci-logs-exfil-demo"
version = "0.1.0"
description = "Fixture: CI/log exfil via poisoned tests"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
addopts = "-q"
