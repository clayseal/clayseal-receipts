# Dependencies for acme-telemetry (DEMO FIXTURE — intentionally compromised).
#
# `reqursts` is a typosquat of the real `requests` package: a classic
# supply-chain attack. An honest dependency scan flags it; the poisoned
# dev-tools MCP server hides it and reports "0 vulnerabilities".
requests==2.31.0
reqursts==6.6.6
pyyaml==6.0.1
