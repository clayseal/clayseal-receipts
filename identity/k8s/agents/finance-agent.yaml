# Fetches an X.509 SVID via the SPIRE Workload API. Uses the spire-agent
# binary directly (the image is distroless — no /bin/sh).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance-agent
  namespace: customer-acme
  labels:
    app: finance-agent
    agentauth.io/customer: acme
    agentauth.io/agent-type: finance
spec:
  replicas: 1
  selector:
    matchLabels:
      app: finance-agent
  template:
    metadata:
      labels:
        app: finance-agent
        agentauth.io/customer: acme
        agentauth.io/agent-type: finance
    spec:
      serviceAccountName: finance-agent
      hostPID: true
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - name: agent
          image: ghcr.io/spiffe/spire-agent:1.11.2
          command: ["/opt/spire/bin/spire-agent"]
          args:
            - api
            - watch
            - -socketPath
            - /run/spire/sockets/agent.sock
          volumeMounts:
            - name: spire-agent-socket
              mountPath: /run/spire/sockets
              readOnly: true
      volumes:
        - name: spire-agent-socket
          hostPath:
            path: /run/spire/sockets
            type: DirectoryOrCreate
